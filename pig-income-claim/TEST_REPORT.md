# 测试报告

测试日期：2026-08-06  
交付版本：V1.0

## 结论

静态网页核心测算逻辑、价格数据构建脚本和桌面端浏览器交互均已通过本地验证。销售期按填写的起止日期逐日计算，开始日和结束日均计入，不要求恰好一个月。

## 自动化测试

| 测试组 | 命令 | 结果 |
|---|---|---|
| 核心赔款计算 | `node --test tests/calculator.test.js` | 32项通过，0项失败 |
| 价格脚本 | `python -m unittest tests/test_price_scripts.py` | 4项通过，0项失败 |
| JavaScript语法 | `node --check assets/js/*.js`（逐文件执行） | 通过 |
| Python语法 | `python -m py_compile scripts/*.py` | 通过 |
| 每日价格映射 | `python scripts/build_daily_prices.py --year 2026` | 生成365天；214天有价，151天待数据 |
| 生产构建 | `npm run build` | 通过 |
| 静态产物验证 | `npm run validate:artifact` | 通过 |

核心逻辑覆盖：六个价差档位及全部边界、价差为0、出栏均重小于/等于/大于120公斤、每头600元封顶、自动/手动目标价格、跨周/跨月/非整月销售期、起止日包含、价格缺失、多销售期、重叠、保险期范围、承保后前30天限制、数量累计和“每头完整精度×数量后再保留两位”的规则。

## 浏览器交互验证

- 完整双销售期案例：10头与20头，本次赔款合计 `1552.99元`；
- 自动/手动目标价格切换，以及手动价格未持久化提示；
- 缺失价格案例显示“部分结果”，列出缺失日期，不形成完整赔款结论；
- 新增、复制、清空、删除销售期均正常；
- 桌面端横向溢出和底部操作条遮挡已修正；
- 打印样式和A4打印样例已生成。

## 数据核验状态

- 目标价格：目前只提供已确认的 `2026年8月 12.84元/公斤`；正式公告链接、公告日期未获得时保持为空，未虚构其他月份。
- 农委周报：已核验至 `2026年第31期`，监测周 `2026-07-27至2026-08-02`，待宰活猪收购均价 `11.64元/公斤`，发布日期 `2026-08-04`。
- 周报抓取脚本的旧版正文解析、新版OCR结果映射、环比交叉校验和失败不写入行为已通过单元测试。
- 当前本地运行环境未安装中文OCR语言包，真实在线OCR抓取未完成闭环验证，因此本报告不宣称自动抓取已经在线成功。GitHub Actions模板会安装 `tesseract-ocr-chi-sim`，首次启用后仍须手动运行并核对日志与新增价格。

## 生产检查

生产构建和静态产物验证均已通过。完整测试用例索引见 `tests/test-cases.md`。
