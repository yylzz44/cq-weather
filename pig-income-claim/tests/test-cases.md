# 计算逻辑测试用例

自动化测试覆盖任务要求中的32类场景，运行命令：

```bash
node --test tests/calculator.test.js
```

重点边界：价差 `0.499999 / 0.5 / 1 / 1.5 / 2 / 2.5`，实际平均出栏重量小于、等于和大于120公斤，每头赔款低于、等于和高于600元，以及“每头完整精度×数量后再保留两位小数”的正确算法。

页面交互另通过浏览器进行新增、复制、清空、删除、重置、自动/手动目标价格切换、错误提示和打印样式检查。
